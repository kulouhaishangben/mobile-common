运行项目的步骤：

1、先下载依赖包：
npm install或者yarn都可以下载；
使用yarn会更安全些，因为yarn有记录依赖包的版本号，而npm有可能下载更新版本的依赖包，有时候会导致项目运行出错。

2、运行项目：
npm start或者yarn start



打包项目：
npm run build或yarn build


注意：在webpack.dev.config.js中，在devServer对象里设置了host属性：
（1）host: "192.168.50.165", // 修改host配置，让服务器外部可访问（这样手机里就能访问了）
（2）但在不同电脑上，需要修改为不同的IP地址！！